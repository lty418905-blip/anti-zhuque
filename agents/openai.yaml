interface:
  display_name: "自然小说审计与文本卫生"
  short_description: "审计中文声线、认知闭环并清理高置信Unicode控制字符"
  default_prompt: "Use $natural-prose-audit to audit this Chinese prose while preserving facts, causality, technical accuracy, and character voice, then run conservative Unicode Layer A only after the final text is frozen."
