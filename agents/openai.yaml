interface:
  display_name: "自然小说审计与文本卫生"
  short_description: "编译结构化正文输入，并审计中文声线与认知闭环"
  default_prompt: "Use $natural-prose-audit to compile my natural-language request into a validated structured writing JSON, write from that frozen input, and audit only after the draft is complete."
